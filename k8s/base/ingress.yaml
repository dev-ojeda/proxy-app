apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: app-react-next-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: sitio-app.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: app-react-next-service
            port:
              number: 80
